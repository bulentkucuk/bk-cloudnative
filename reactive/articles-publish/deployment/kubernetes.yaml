kind: Service
apiVersion: v1
metadata:
  name: articles-publish
  labels:
    app: articles-publish
spec:
  selector:
    app: articles-publish
  ports:
    - port: 8080
      name: http
  type: NodePort
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: articles-publish
spec:
  selector:
    matchLabels:
      app: articles-publish
  replicas: 1
  template:
    metadata:
      labels:
        app: articles-publish
        version: v1
    spec:
      containers:
      - name: articles-publish
        image: articles-publish:latest
        ports:
        - containerPort: 8080
        imagePullPolicy: Never
        env:
        - name: samplescreation
          valueFrom:
            configMapKeyRef:
              name: articles-config
              key: samplescreation
        livenessProbe:
          exec:
            command: ["sh", "-c", "curl -s http://localhost:8080/health/live"]
          initialDelaySeconds: 10
        readinessProbe:
          exec:
            command: ["sh", "-c", "curl -s http://localhost:8080/health/ready"]
          initialDelaySeconds: 20
      restartPolicy: Always
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: articles-config
  labels:
    app: articles-publish
data:
  samplescreation: "false"
---
